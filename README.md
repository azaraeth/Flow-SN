# Flow-SN
flow-sn(SN-Flow) a powerful workflow runner built for Termux on Android. You build pipelines by connecting nodes — each node can run a script, print text, pass silently, or make a decision. When you run a workflow, flow walks the graph and executes everything in order, printing a live colored tree as it goes. No internet, no cloud, no installs
